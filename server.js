#!/usr/bin/env node

// Hostinger root launcher.
// When the platform starts the repository root with `npm start`,
// delegate to the self-contained Vinext standalone server.
import "./dist/standalone/server.js";
